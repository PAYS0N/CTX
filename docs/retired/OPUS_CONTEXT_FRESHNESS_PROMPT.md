You are a senior Rust systems engineer joining the CTX project — an opinionated
agentic-coding system whose own doctrine is "every `must` is either generated
from its source of truth or checked by the battery." An external audit
(`docs/AUDIT.md`) found that this doctrine is applied to Rust code but not to
the project's own prose: several always-injected docs (`intent.md`, `README.md`,
`CLAUDE.md`, `docs/SPEC.md`) restate facts that live in code and have drifted
from it. Because CTX's hook injects this prose into a low-reasoning model's
context on every file read, a wrong doc is worse than no doc — it actively
misleads. Your job is to close that gap for three specific mechanisms. Do not
attempt the audit's other findings; they are out of scope for this pass (see
`<out_of_scope>`).

Read `docs/AUDIT.md` in full before starting — it is the rationale for every
instruction below and cites exact file:line locations. Treat its claims as a
starting hypothesis, not fact: re-verify each one against current source
before acting on it, since the repo may have moved since the audit was written.

<context>
Confirmed still true as of this prompt being written:
- `.context/intent.md` (owner-authored, injected on nearly every read) states
  "Layer 2 — ctx-access is the sanctioned path to source (chain read
  root→target, write-needs-read, task lifecycle)". This architecture was
  deleted in ADR-035/036. `docs/SPEC.md`'s Non-goals section says "Editing
  intent.md after directory creation" is a non-goal — i.e. intent.md is meant
  to be frozen. Flag any edit here as needing explicit owner sign-off in your
  final report; don't silently treat "frozen" as overridden.
- `README.md` lines 27-36 document a `ctx-access init-task`/`end-task`
  quickstart that doesn't exist anymore.
- `CLAUDE.md` (root and `template/`) says `ctx-verify`'s checkpoint is
  `{"status":"pass"}`. Actual behavior
  (`crates/ctx-verify/src/cli.rs`): the default terse render prints the bare
  word `pass`; the JSON envelope only appears behind `--json`. Just 'pass' is 
  canonically correct.
- Literal `ctx-access` still appears in: `crates/ctx-cage/src/cli.rs` (help
  text, `about =` and a self-test doc comment), `crates/ctx-summarize/src/
  cpath.rs`, `crates/ctx-summarize/src/cache.rs` (dead `FromCache` mode reading
  a cache path nothing writes anymore), `crates/ctx-verify/src/runner.rs`,
  `crates/ctx-verify/src/lib.rs`, `crates/ctx-core/src/lib.rs` — plus
  `README.md` and `intent.md`. Re-grep before you fix anything; this list may
  be incomplete or stale.
- `docs/SPEC.md` uses a "sealed body + rev-4 amendment paragraph" pattern: the
  amendment says it "supersedes, in the body below" several whole sections
  (Access protocol, summarization trigger, file-format prose), but the
  superseded prose is still there, unedited, for a literal reader to trip on.
- `docs/STATUS.md`'s "Current shape (post-pivot)" section duplicates
  architecture description that a regenerated root `rollup.ctx` could carry
  instead (rollups are regenerated by `ctx-scan --update`; STATUS.md is
  hand-maintained and drifts because nothing forces it back in sync).
- `crates/ctx-context/src/serve.rs` (`serve_one`, `chain_for`, `fresh_chain_for`)
  serves chain nodes from disk with an `(absent: …)` marker for missing files,
  but never consults `.context/<dir>/hashes.json`. Staleness is already fully
  computable: `crates/ctx-scan/src/hash.rs` has `TreeState`, `DirNode`, and a
  `Staleness` struct (`stale_dirs`, `changed_files`, `orphan_leaves`) built
  exactly for this. `ctx-scan --check` uses it read-only today; `ctx-context`
  (the thing actually injected into the model's window) does not.
- `.claude/settings.json`'s hook matcher is `Read|Grep|Glob` (no `Edit|Write`).
  Not in scope to fix here, but don't let Goal 3's staleness signal assume the
  hook fires more often than it does.
</context>

<out_of_scope>
Do not fix in this pass (each is a real audit finding, but not one of the
three problems this prompt targets — fixing them would dilute the diff and
make review harder): dead `FromCache` removal beyond what Goal 1 requires,
`stop_reason` truncation check in `agents/summarizer-claude.py`, mechanical
validation of generated `.ctx`/rollup output (banned-phrase grep, line-budget
enforcement), `cargo-machete`/`ToolMissing` silently-passing skip status,
`-p`/`--workspace` scoping bug in `ctx-verify`'s doc/test checks,
`rationale_check.py`'s doc-comment blind spot, `.claude/settings.local.json`
missing from `.ctxignore`. If you notice a one-line fix directly blocks one of
the three goals below, take it and say so in your report; otherwise leave it
and list it as a follow-up.
</out_of_scope>

<instructions>
Work in three phases. Run `target/debug/ctx-verify` (whole workspace) after
each phase and do not start the next phase on a failing one. Think through the
design of each mechanism before writing code — these are structural decisions
(what a `--contract` flag should print, where generation lives, what "stale"
means for a chain node) that are easy to get subtly wrong and expensive to
redo.

**Phase 1 — Make the tool-contract docs generated, not restated; retire
`ctx-access` from live code and help text.**

1. Give each user-facing binary (`ctx-context`, `ctx-verify`, `ctx-scan`,
   `ctx-cage`) a way to print its own one-paragraph agent-facing contract —
   either a `--contract` flag or (if you judge `--help`'s existing `about`/doc
   comments already are that contract, once corrected) reuse `--help`. Decide
   which and justify it in your report; don't do both. Whichever you pick, the
   text must always be *factually exact*, e.g. `ctx-verify`'s contract must say the
   default render prints the bare word `pass`, and that `{"status":"pass"}`
   requires `--json` — or, if you judge it's better to make the JSON envelope
   the default and keep terse behind a flag instead, do that and update the
   contract to match. Pick one direction; don't leave the mismatch under a
   caveat.
2. Add a small generator (fits the existing `scripts/` idiom — bash calling
   each binary and concatenating output between markers, no new crate needed
   unless you find a concrete reason one is) that assembles a canonical
   "tool contracts" block from the binaries' own `--contract`/`--help` output.
3. Wire generation to run "regularly, in sync with current context gen" per
   `CLAUDE.md`'s own instruction — i.e. hang it off the same trigger as
   `ctx-scan --update` (a flag on that command, or a script it calls) rather
   than inventing a separate cadence.
4. Add a `ctx-verify` battery check that asserts the committed tool-contract
   sections in `CLAUDE.md`/`README.md` (both root and `template/`, per the
   existing "mirror" doctrine) exactly match freshly-generated output. A
   contract change that doesn't regenerate the doc must fail the checkpoint.
   Delimit the generated block clearly (e.g. HTML comment markers) so the rest
   of each file stays hand-authored.
5. Replace `README.md`'s dead quickstart (lines 27-36) and `CLAUDE.md`'s
   tool-usage bullets with the generated block.
6. Add a small `retired_terms_check.sh` (same enumeration convention as the
   existing `scripts/` checks — `git ls-files`, not a filesystem walk) that
   greps for a short banned-identifier list (`ctx-access`, `init-task`,
   `end-task`, `served_nodes`) and fails if found anywhere outside
   `docs/DECISIONS.md` and `docs/retired/**`. Wire it into `ctx-verify`'s
   battery. This is what makes "the incorrect tool names error should never
   happen" mechanically true instead of a one-time cleanup. For every failure,
   `ctx-access` string in `crates/**/*.rs` (doc comments, `about =`, cli help,
   the dead `cache.rs`/`FromCache` path in `ctx-summarize`) remove or
   correctly rename each occurrence — none of it should survive as live code
   or `--help` text; historical mentions belong only in `docs/DECISIONS.md`
   and (after Phase 2) `docs/retired/`.

7. Rewrite `.context/intent.md`'s body: strip mechanism/architecture
   description entirely (the "ctx-access is the sanctioned path..." bullet and
   similar) and restate each Layer purely as a goal or invariant that would
   survive a future pivot (e.g. "force agents to read project context
   top-down before acting on any file" with no tool named). Point to
   `docs/SPEC.md`/`docs/STATUS.md` for current mechanism, as the file already
   does for rationale. Call out in your final report that this touches a file
   `docs/SPEC.md` lists as a non-goal to edit post-creation, so the owner
   should explicitly confirm it before merge.

**Phase 2 — Restructure `docs/` around temporal role, not sealed-plus-amendment.**

Below is a description of the inital doc strategy. Before implementing, think about the role 
of SPEC and STATUS against .ctx, and see what value they have and how it can be ensured 
without drift. If the below is a good way to enforce that, continue. otherwise, 
re-propose a different solution.

1. `docs/SPEC.md`: rewrite the body to *currently be true* — delete or rewrite
   the stale prose the rev-4 amendment says it supersedes (Access protocol
   section, summarization trigger, file-format prose) rather than leaving both
   old and new text for a literal reader to reconcile. Keep "sealed — changes
   require an explicit re-spec" as the *process* rule (an ADR is still
   required to change it), but the document itself should read as one
   coherent, current spec, not a base layer plus a patch. Move the
   rev-history narrative (what rev 1/2/3/4 changed and why) fully into
   `docs/DECISIONS.md` if it isn't already adequately covered there (ADR-035
   is close; check for gaps) — SPEC states *what is true now*, DECISIONS
   states *why/what changed*.
2. `docs/STATUS.md`: trim the "Current shape (post-pivot)" section to remove
   anything a regenerated root `.context/rollup.ctx` already states (tool
   names, hook wiring, freshness model, cage posture) — keep only what a
   rollup *can't* express: active/open work, dates, what's blocked on what,
   deferred items. Add a one-line pointer at the top: "for current
   architecture, read the root rollup via `ctx-context .`", so STATUS.md
   stops being a second copy of the same facts.
3. Move `docs/SANDBOX.md` to `docs/retired/SANDBOX.md` (create the directory).
   Fix any inbound references (`docs/DECISIONS.md`'s `[[ADR-NNN]]` links,
   SPEC.md, STATUS.md, code comments) to point at the new path or drop the
   reference if it's purely historical.
4. Leave `docs/UNIMPLEMENTED.md` and `docs/DYLINT_RULES.md` as-is unless your
   Phase 1 `ctx-access` grep turns up hits in them — fix only those hits.

**Phase 3 — Surface staleness at the point context is served, not just at
`ctx-scan --check` time.**

1. In `crates/ctx-context/src/serve.rs`, extend `serve_one` (or add a
   thin wrapper the existing call sites use) so each served node is checked
   against its recorded hash in the relevant `.context/<dir>/hashes.json`
   sidecar. Reuse `ctx_scan::hash`'s existing `TreeState`/`Staleness`
   machinery — don't reimplement hashing. If depending on `ctx-scan` from
   `ctx-context` would invert the crates' current dependency direction (check
   `crates/*/Cargo.toml` before deciding), that's a sign the hashing logic is
   in the wrong crate, not a reason to duplicate it: move it to somewhere both
   can depend on cleanly (e.g. `ctx-core`) instead.
2. When a served node's file hash doesn't match the sidecar's recorded hash
   (or the sidecar itself doesn't have an entry for a file that exists — the
   "never generated" case, audit finding #11), prefix that node's rendered
   section with an explicit one-line marker distinguishing the two cases,
   e.g. `[STALE — source changed since last regen]` vs.
   `[NEVER GENERATED — no rollup on record]`, so the difference between
   "outdated" and "was never built" is visible to the consuming model — don't
   collapse them into one banner.
3. This must apply to both paths that call `chain_for`/`fresh_chain_for`:
   the `--hook` mode and direct `ctx-context <path>` CLI mode — implement it
   once, low in the shared serving path, not duplicated per caller.
4. Extend `ctx-scan --check`'s reporting (audit finding #11) so it also
   flags directories/files whose expected `.ctx`/`rollup.ctx` artifact is
   simply absent (never generated), not only ones whose hash mismatches — if
   `Staleness` doesn't already distinguish these, add the field.
5. Add hermetic tests using the project's existing fake-`Env` pattern: (a) a
   fixture where a source file's content postdates its sidecar-recorded hash
   → assert the served node carries the STALE marker; (b) a fixture where a
   directory has source but no `rollup.ctx`/sidecar entry at all → assert it's
   marked distinctly from both "absent-and-never-existed-as-a-concept" (the
   existing `(absent: …)` marker for e.g. missing `intent.md`) and from
   "stale". Get the semantic distinction right: `(absent: …)` today means "no
   such node exists to have"; your new markers mean "a node exists but its
   content is untrustworthy" — don't conflate the two in the render.
6. Update `docs/SPEC.md`'s Layer 2 section and `docs/STATUS.md`'s pointer
   (from Phase 2) to describe this behavior once it exists, so it doesn't
   become the next finding-#1-style drift the moment it ships.
</instructions>

<constraints>
- Verify each audit and prompt (this text) claim against current source before acting on it — the
  audit is grounding, not ground truth. If you find a claim is already fixed
  or was inaccurate, say so in your report rather than re-fixing a non-issue.
- Where the audit or this prompt leaves a design choice open (contract format,
  which crate owns shared staleness logic, `--contract` vs. corrected
  `--help`), make the call yourself or raise as a question depending on the complexity.
- `intent.md` and sealed-`SPEC.md` edits touch files this repo's own doctrine
  says require explicit owner sign-off before a directory-creation-time
  document changes, or an ADR before a sealed spec changes. Make the edits
  (that's the task), but call both out explicitly and separately in your
  final report as "needs owner confirmation" rather than treating your own
  edit as sufficient authorization. Add the ADR entry to `docs/DECISIONS.md`
  yourself for the SPEC.md rewrite (that's the mechanical part of "sealed" —
  a recorded revision note); the intent.md sign-off is a human step you
  cannot substitute for.
</constraints>

<output_format>
For each phase: the diff (via normal file edits), then a short per-phase note
(3-6 sentences) covering what you changed, any open design choices you made
and why, and the `ctx-verify` result. At the end, one consolidated section
titled "Needs owner confirmation" listing exactly the intent.md rewrite and
the SPEC.md re-spec (with the ADR number you added) Do not produce a
separate planning document — the per-phase notes are the record.
</output_format>
