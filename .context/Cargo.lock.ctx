Auto-generated Cargo dependency lockfile for this workspace; not hand-edited. Pins exact versions/checksums for the full transitive dependency graph of the workspace crates `ctx-cage`, `ctx-context`, `ctx-core`, `ctx-scan`, `ctx-summarize`, and `ctx-verify`.

Notable direct dependencies per crate (visible in each `[[package]]` block's `dependencies` list): `ctx-core` uses `serde` + `sha2` (hashing for context tree); `ctx-scan` and `ctx-summarize` pull in `ignore` (gitignore-aware directory walking) and `clap`; `ctx-cage` uses `nix`/`rustix` for sandboxing syscalls; all crates share `serde_json` and `thiserror` for error handling/serialization.

Editing rule: never hand-edit — regenerate via `cargo update`/`cargo build`. If a workspace crate's `Cargo.toml` dependencies change, this file must be regenerated or builds become inconsistent with the checked-in lock. The `version = 4` lockfile format field must match the Cargo version used in CI. No code lives here; irrelevant to logic changes unless a dependency bump is the cause of a bug (e.g., check `regex-syntax`, `serde`, `zmij` versions if behavior changes trace to a dependency).